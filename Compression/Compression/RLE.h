#pragma once
#include <iostream>
#include <string>
#include <queue>
using namespace std;

bool checkNext(queue <char>& ochered);

string RLEcompress(string& text);

string RLEdecompress(string code);

